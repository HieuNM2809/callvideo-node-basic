<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Video Call</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css">
    <style>
        body {
            font-family: Arial, sans-serif;
            text-align: center;
            background-color: #f0f2f5;
            margin: 0;
            padding: 0;
        }

        .container {
            margin-top: 50px;
        }

        .header {
            background-color: #4267B2;
            color: #fff;
            padding: 20px 0;
        }

        .content {
            background-color: #fff;
            border-radius: 8px;
            box-shadow: 0 0 8px rgba(0, 0, 0, 0.1);
            padding: 20px;
            margin-top: 20px;
        }

        input[type="text"] {
            padding: 10px;
            margin-right: 10px;
            border: 1px solid #ddd;
            border-radius: 4px;
            width: calc(100% - 100px);
            font-size: 16px;
        }

        input[type="submit"] {
            padding: 10px 20px;
            background-color: #4267B2;
            color: white;
            border: none;
            cursor: pointer;
            border-radius: 4px;
            font-size: 16px;
        }

        input[type="submit"]:hover {
            background-color: #3b5998;
        }

        .input-group {
            display: flex;
            margin-bottom: 20px;
            align-items: center;
            justify-content: center;
        }

        .input-group i {
            margin-right: 10px;
            color: #4267B2;
            font-size: 20px;
        }

        .input-group input[type="text"]::placeholder {
            color: #999;
        }

        .input-group input[type="text"]:-ms-input-placeholder {
            color: #999;
        }

        .input-group input[type="text"]::-ms-input-placeholder {
            color: #999;
        }
    </style>
</head>

<body>
    <div class="header">
        <h1>UUID của bạn</h1>
        <b>
            <%= uuid || 'N/A' %>
        </b>
    </div>
    <div class="container">
        <div class="content">
            <div class="input-group">
                <i class="fas fa-key"></i>
                <input type="text" name="inputRoom" id="inputRoom" placeholder="Mã phòng">
                <input type="submit" id="create" value="Tạo phòng">
            </div>
            <i style="color: #999;">Lưu ý: Mã phòng giá trị là UUID của bạn bè hoặc chữ số bất kì</i>
        </div>
    </div>

    <script>
        document.addEventListener("DOMContentLoaded", function () {
            const createButton = document.getElementById("create");
            const inputRoomInput = document.getElementById("inputRoom");

            createButton.addEventListener("click", function () {
                const inputRoomValue = inputRoomInput.value;
                if (inputRoomValue) {
                    window.location.href = `/${inputRoomValue}`;
                } else {
                    alert("Vui lòng nhập mã phòng");
                }
            });
        });
    </script>
</body>

</html>